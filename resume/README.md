# Resume Generation

```
npm install resumed jsonresume-theme-full
cd node_modules
rm -rf jsonresume-theme-full
git clone https://github.com/acesaro/jsonresume-theme-full.git
cd -
node node_modules/resumed/bin/resumed.js --theme jsonresume-theme-full
```
